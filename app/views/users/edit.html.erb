<% if not @user == current_user %>
<h1> <%= "Ne ofigel?" %> </h1>
<% else %>
<% if @user.errors.any? %>
<ul>
  <% @user.errors.full_messages.each do |m| %>
  <li><%= m %></li>
</ul>
  <% end %>

<% end %>

<%= form_for @user do |f| %>




  <%= f.text_field :login %>
  <%= f.text_field :email %>
  <%= f.password_field :password %>
  <%= f.submit "Sign_up" %>

<% end %>
<% end %>
